namespace DevLifeBackend.Enums
{
     [Flags] 
     public enum TechStack
        {
            
            Unknown = 10,
            NET = 20,
            React = 30,
            Python = 40,
            Angular = 50

    }
}
